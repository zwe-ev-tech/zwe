export enum Status {
    Active = "active",
    Inactive = "inactive",
}

export enum FileUploadErrorType {
    Failed = "failed",
    Invalid_Mine_Types = "invalid_mine_types",
    Invalid_Dimensions = "invalid_dimensions"
}